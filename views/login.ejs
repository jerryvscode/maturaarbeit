<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WebApp</title>
    <link rel="stylesheet" href="/general.css">
    <link rel="stylesheet" href="/header.css">
    <link rel="stylesheet" href="/account.css">
  </head>
  
  <body>
    <!-- Header -->
    <%- include("includes/header") %>
    <main>
      <div class="login-div">

        <!---->
        <form action="/login" method="POST">
          <% errors.forEach(error => { %>
          <p class="notice"><%= errors %></p>
          <% }) %>
          <!---->
          <div class="login-form">
            <legend class="login-register-legend">Einloggen</legend>
            <input class="account-input" id="username" name="username" placeholder="Benutzername...">
            <input class="account-input" id="password" name="password" type="password" placeholder="Passwort..."></input>
            <button class="send-button">Einloggen</button>
          </div>
        </form>
      </div>
    </main>
    <!-- Footer -->
    <%- include("includes/footer") %>
  </body>
</html>